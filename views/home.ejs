<%- include ("partials/header"); -%>


<h1>Home</h1>
<p><%= startingContent %></p>



<% posts.forEach(post => { %>

    <h1><%= post.title %></h1>
    <p class="truncatedText"><%= post.content.substring(0, 100) + "..." %>
    <a href= "/posts/<%= _.lowerCase(post.title)%>" >Read More</a>
    </p>



<% }); %>

<%- include ("partials/footer"); -%>




<%#  let truncatedText = truncateText(post.content, 100) %>
